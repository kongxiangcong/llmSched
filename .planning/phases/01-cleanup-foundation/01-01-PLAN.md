---
phase: 01-cleanup-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/llm_sched/analysis/
  - src/llm_sched/visualization/
  - src/llm_sched/tools/
  - scripts/generate_diagnosis_baselines.py
  - scripts/run_end_to_end.py
  - docs/plans/
  - docs/development/
  - docs/architecture-diagnosis/
  - src/llm_sched/compare_grouping.py
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
must_haves:
  truths:
    - "src/llm_sched/analysis/ directory no longer exists"
    - "src/llm_sched/visualization/ directory no longer exists"
    - "src/llm_sched/tools/ directory no longer exists"
    - "scripts/run_end_to_end.py no longer exists"
    - "scripts/generate_diagnosis_baselines.py no longer exists"
    - "docs/plans/ directory no longer exists"
    - "docs/development/ directory no longer exists"
    - "docs/architecture-diagnosis/ directory no longer exists"
    - "src/llm_sched/compare_grouping.py no longer exists"
  artifacts:
    - path: "src/llm_sched/"
      provides: "Clean package root with wholesale-deleted modules removed"
      absent_dirs:
        - "analysis"
        - "visualization"
        - "tools"
  key_links:
    - from: "git rm commands"
      to: "working tree"
      via: "git status shows deleted files staged"
---

<objective>
Execute wholesale deletions of entire directories and files that are unambiguously v0.9-era and out of scope for v2.

Purpose: Remove the largest, cleanest deletion targets first to reduce codebase surface before selective pruning.
Output: Staged git deletions for analysis/, visualization/, tools/, scripts/, docs/plans/, docs/development/, docs/architecture-diagnosis/, and compare_grouping.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-cleanup-foundation/01-CONTEXT.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

## Decisions Implemented
- D-01: Delete whole directories where possible (analysis/, visualization/, tools/)
- D-02: One big cleanup commit for all deletions
- D-06: docs/development/ and docs/architecture-diagnosis/ deleted (git status confirms)
- D-07: docs/plans/ deleted
- D-08: scripts/run_end_to_end.py deleted; scripts/generate_diagnosis_baselines.py also deleted
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wholesale delete v0.9-era directories and files</name>
  <files>
    src/llm_sched/analysis/
    src/llm_sched/visualization/
    src/llm_sched/tools/
    scripts/generate_diagnosis_baselines.py
    scripts/run_end_to_end.py
    docs/plans/
    docs/development/
    docs/architecture-diagnosis/
    src/llm_sched/compare_grouping.py
  </files>
  <read_first>
    - src/llm_sched/analysis/ (confirm 21 files, all to delete)
    - src/llm_sched/visualization/ (confirm 5 files, all to delete)
    - src/llm_sched/tools/ (confirm 2 files, all to delete)
    - scripts/ (confirm generate_diagnosis_baselines.py and run_end_to_end.py exist)
    - docs/plans/ (confirm old plan markdown files exist)
    - docs/development/ (confirm exists, to be deleted per D-06)
    - docs/architecture-diagnosis/ (confirm exists, to be deleted per D-06)
    - src/llm_sched/compare_grouping.py (confirm exists, only referenced by modules being deleted)
  </read_first>
  <action>
    Run the following git rm commands in order. Each command removes a target that is confirmed to have zero consumers among modules being retained:

    1. `git rm -r src/llm_sched/analysis/` — 21 files, all diagnosis/report/evaluation builders. No retained module imports from analysis/.
    2. `git rm -r src/llm_sched/visualization/` — 5 files, all catalog/workbench builders. No retained module imports from visualization/.
    3. `git rm -r src/llm_sched/tools/` — 2 files, end_to_end_runner.py and __init__.py. No retained module imports from tools/.
    4. `git rm scripts/generate_diagnosis_baselines.py` — standalone script, no consumers.
    5. `git rm scripts/run_end_to_end.py` — standalone script, no consumers.
    6. `git rm -r docs/plans/` — old execution plan markdown files, no consumers.
    7. `git rm -r docs/development/` — old development handoff docs, no consumers.
    8. `git rm -r docs/architecture-diagnosis/` — old architecture diagnosis docs, no consumers.
    9. `git rm src/llm_sched/compare_grouping.py` — only referenced by analysis/, pipeline/, contracts/ modules that are all being deleted. After this plan and Plan 02, no consumer remains.

    After all deletions, run `git status` to confirm the expected files are staged for deletion.
  </action>
  <verify>
    <automated>
      test -d src/llm_sched/analysis && echo "FAIL: analysis still exists" && exit 1 || echo "OK: analysis deleted"
      test -d src/llm_sched/visualization && echo "FAIL: visualization still exists" && exit 1 || echo "OK: visualization deleted"
      test -d src/llm_sched/tools && echo "FAIL: tools still exists" && exit 1 || echo "OK: tools deleted"
      test -f scripts/generate_diagnosis_baselines.py && echo "FAIL: generate_diagnosis_baselines.py still exists" && exit 1 || echo "OK: generate_diagnosis_baselines.py deleted"
      test -f scripts/run_end_to_end.py && echo "FAIL: run_end_to_end.py still exists" && exit 1 || echo "OK: run_end_to_end.py deleted"
      test -d docs/plans && echo "FAIL: docs/plans still exists" && exit 1 || echo "OK: docs/plans deleted"
      test -d docs/development && echo "FAIL: docs/development still exists" && exit 1 || echo "OK: docs/development deleted"
      test -d docs/architecture-diagnosis && echo "FAIL: docs/architecture-diagnosis still exists" && exit 1 || echo "OK: docs/architecture-diagnosis deleted"
      test -f src/llm_sched/compare_grouping.py && echo "FAIL: compare_grouping.py still exists" && exit 1 || echo "OK: compare_grouping.py deleted"
      git diff --cached --name-only | grep -E "src/llm_sched/analysis|src/llm_sched/visualization|src/llm_sched/tools|scripts/generate_diagnosis_baselines.py|scripts/run_end_to_end.py|docs/plans|docs/development|docs/architecture-diagnosis|src/llm_sched/compare_grouping.py" | wc -l
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -d src/llm_sched/analysis` returns false
    - `test -d src/llm_sched/visualization` returns false
    - `test -d src/llm_sched/tools` returns false
    - `test -f scripts/generate_diagnosis_baselines.py` returns false
    - `test -f scripts/run_end_to_end.py` returns false
    - `test -d docs/plans` returns false
    - `test -d docs/development` returns false
    - `test -d docs/architecture-diagnosis` returns false
    - `test -f src/llm_sched/compare_grouping.py` returns false
    - `git diff --cached --name-only` contains deleted paths for all nine targets
  </acceptance_criteria>
  <done>All nine wholesale deletion targets are removed from the working tree and staged in git.</done>
</task>

<task type="auto">
  <name>Task 2: Commit wholesale deletions</name>
  <files>.git/</files>
  <read_first>
    - git status output from Task 1
  </read_first>
  <action>
    Create a single commit with the message exactly as specified in D-02:

    `git commit -m "cleanup: remove v0.9-era analysis, diagnosis, visualization, evaluation"`

    If the commit fails due to pre-commit hooks, investigate and fix the issue (do not use --no-verify).
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -q "cleanup: remove v0.9-era analysis, diagnosis, visualization, evaluation"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --oneline` contains the exact substring "cleanup: remove v0.9-era analysis, diagnosis, visualization, evaluation"
    - `git status` shows a clean working tree (no unstaged changes from this plan)
  </acceptance_criteria>
  <done>Wholesale deletions committed as a single atomic cleanup commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree -> git index | Deletions are staged before commit; reversible until commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | Working tree | accept | Deletions are staged; `git reset HEAD` can recover before commit. After commit, `git revert` or `git checkout HEAD~1 -- <path>` can restore. |
</threat_model>

<verification>
1. All nine targets are absent from the filesystem.
2. `git log -1` shows the cleanup commit.
3. No retained module references any deleted path (verified by grep in Plan 02).
</verification>

<success_criteria>
- src/llm_sched/analysis/ deleted
- src/llm_sched/visualization/ deleted
- src/llm_sched/tools/ deleted
- scripts/generate_diagnosis_baselines.py deleted
- scripts/run_end_to_end.py deleted
- docs/plans/ deleted
- docs/development/ deleted
- docs/architecture-diagnosis/ deleted
- src/llm_sched/compare_grouping.py deleted
- All deletions in one commit
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup-foundation/01-01-SUMMARY.md`
</output>
