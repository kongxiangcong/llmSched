---
phase: 01-cleanup-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-03b
  - 01-04
files_modified:
  - README.md
  - pyproject.toml
autonomous: true
requirements:
  - CLEAN-03
  - CLEAN-04
must_haves:
  truths:
    - "README states the project is undergoing complete refactoring"
    - "README states current outputs are not correct"
    - "README describes the 6-phase roadmap"
    - "README has no v0.9-specific content"
    - "README has no Chinese text"
    - "README has no diagnosis, visualization, sweep analysis, compare, or evaluation report references"
    - "README has no close-enough or practical stop-line language"
    - "numpy is in project dependencies"
  artifacts:
    - path: "README.md"
      provides: "v2 README"
      contains:
        - "undergoing complete refactoring"
        - "current outputs are not correct"
        - "6-phase roadmap"
      absent:
        - "M1"
        - "M2"
        - "SPEC-13"
        - "SPEC-14"
        - "SPEC-15"
        - "SPEC-16"
        - "SPEC-19"
        - "已完成"
        - "当前"
        - "核心 CLI 流水线"
        - "diagnosis"
        - "visualization"
        - "sweep analysis"
        - "compare"
        - "evaluation reports"
        - "close-enough"
        - "practical stop-line"
    - path: "pyproject.toml"
      provides: "Updated dependencies"
      contains: '"numpy'
---

<objective>
Rewrite README.md for v2 scope per D-25. Add numpy to dependencies per D-23. Keep Python >=3.11 per D-24.

Purpose: Communicate the project's new direction and ensure dependencies are ready for Phase 2 frontend work.
Output: Complete v2 README and updated pyproject.toml with numpy dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-cleanup-foundation/01-CONTEXT.md
@.planning/PROJECT.md

## Decisions Implemented
- D-23: Add numpy to dependencies for tensor manipulation during ONNX frontend import.
- D-24: Keep Python >=3.11 in pyproject.toml.
- D-25: Full rewrite with v2 scope. Describe the ONNX → task DAG → scheduling → descriptor packing pipeline, the 6-phase roadmap, and current status. Remove all v0.9-specific content and Chinese text.

## Target README Content
1. Project title and one-line description
2. Warning banner: "This project is undergoing complete refactoring. Current outputs are not correct."
3. What this is: ONNX LLM → v0.10 descriptor compiler for tars-npu-ctrl
4. 6-phase roadmap with current status (Phase 1 in progress)
5. Tech stack
6. Quick start (compile command stub)
7. Input constraints (ONNX only, dual-core only, no layout ops)
8. Development workflow (GSD)
9. License placeholder

## What to REMOVE from current README
- All Chinese text
- All v0.9-era CLI command lists
- All SPEC-13/14/15/16/19 references
- All M1/M2 milestone references
- All evaluation report references
- All test strategy details (will be rewritten in Plan 06)
- All architecture-diagnosis references
- All "close-enough / practical stop-line" language
- All diagnosis, visualization, sweep analysis, compare, evaluation reports language
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md</name>
  <files>
    README.md
  </files>
  <read_first>
    - README.md (current content, to understand what to remove)
    - .planning/ROADMAP.md (for phase descriptions)
    - .planning/PROJECT.md (for core value and constraints)
  </read_first>
  <action>
    Write README.md with the following exact structure and content. Do not include any Chinese text. Do not include any v0.9-specific content.

    ```markdown
    # llmSched v2

    > **Warning**: This project is undergoing complete refactoring. Current outputs are not correct. Do not use for production.

    A compiler that transforms ONNX LLM models into verified v0.10 descriptor sets for the `tars-npu-ctrl` NPU controller.

    ## What This Is

    Given an ONNX model, llmSched v2 produces a correct, verifiable v0.10 descriptor set that the NPU controller can parse and execute. Round-trip verification must pass for every descriptor.

    The pipeline:

    ```
    ONNX model → Task DAG → Scheduling → v0.10 Descriptor Packing → Structural Metrics
    ```

    ## Roadmap

    The project is organized into 6 phases:

    1. **Cleanup & Foundation** — Remove old v0.9-era code and docs, restructure packages (in progress)
    2. **Task DAG Frontend** — Extract task-unit dependency DAG from ONNX models
    3. **Task-Centric Scheduling** — Schedule task DAGs with memory planning and execution ordering for dual-core NPU
    4. **Descriptor Packing** — Pack and parse v0.10 descriptors for all 11 families
    5. **Descriptor Verification** — Harden correctness with round-trip and 4-layer verification gate
    6. **Performance Metrics** — Emit structural metrics and per-layer timelines

    ## Tech Stack

    - Python 3.11+
    - Pydantic v2 (schema validation)
    - Typer (CLI)
    - ONNX (model import)
    - NumPy (tensor manipulation)

    ## Quick Start

    ```bash
    # Install
    pip install -e ".[dev]"

    # Compile (stub — full pipeline wires in Phase 2)
    llm-sched compile model.onnx --config config.yaml --output ./out/
    ```

    ## Input Constraints

    - **Input format**: ONNX only
    - **Target hardware**: Dual-core NPU with independent per-core VMEM
    - **Ignored ops**: reshape, transpose, squeeze, unsqueeze, gather (transparent to NPU)
    - **Descriptor version**: v0.10 (0x6) only — no backward compatibility

    ## Development Workflow

    This project uses the GSD (Get Shit Done) workflow:

    - `.planning/PROJECT.md` — Living project context
    - `.planning/REQUIREMENTS.md` — Scoped requirements with REQ-IDs
    - `.planning/ROADMAP.md` — 6-phase execution roadmap
    - `.planning/STATE.md` — Current position and progress

    ## License

    TBD
    ```

    The file must:
    - Start with the warning banner
    - Have no Chinese characters
    - Have no references to SPEC-13, SPEC-14, SPEC-15, SPEC-16, SPEC-19, M1, M2, v0.9, evaluation reports, diagnosis, visualization, sweep analysis, compare, close-enough, practical stop-line
    - Reference the 6-phase roadmap
    - Include the compile command stub
  </action>
  <verify>
    <automated>
      grep -q "undergoing complete refactoring" README.md && echo "OK warning present" || (echo "FAIL" && exit 1)
      grep -q "current outputs are not correct" README.md && echo "OK warning present" || (echo "FAIL" && exit 1)
      grep -q "6 phases" README.md && echo "OK roadmap present" || (echo "FAIL" && exit 1)
      grep -q "llm-sched compile" README.md && echo "OK compile command present" || (echo "FAIL" && exit 1)
      grep -qi "SPEC-13\|SPEC-14\|SPEC-15\|SPEC-16\|SPEC-19\|M1\|M2\|v0.9" README.md && echo "FAIL old refs present" && exit 1 || echo "OK no old refs"
      grep -qi "diagnosis" README.md && echo "FAIL diagnosis ref present" && exit 1 || echo "OK no diagnosis refs"
      grep -qi "visualization" README.md && echo "FAIL visualization ref present" && exit 1 || echo "OK no visualization refs"
      grep -qi "sweep analysis" README.md && echo "FAIL sweep analysis ref present" && exit 1 || echo "OK no sweep analysis refs"
      grep -qi "compare" README.md && echo "FAIL compare ref present" && exit 1 || echo "OK no compare refs"
      grep -qi "evaluation reports" README.md && echo "FAIL evaluation reports ref present" && exit 1 || echo "OK no evaluation reports refs"
      grep -qi "close-enough" README.md && echo "FAIL close-enough ref present" && exit 1 || echo "OK no close-enough refs"
      grep -qi "practical stop-line" README.md && echo "FAIL practical stop-line ref present" && exit 1 || echo "OK no practical stop-line refs"
      python3 -c "import re; content=open('README.md').read(); chinese=re.search(r'[一-鿿]', content); print('FAIL chinese present') if chinese else print('OK no chinese')" || true
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "undergoing complete refactoring"
    - README.md contains "current outputs are not correct"
    - README.md contains "6 phases" or "6-phase"
    - README.md contains "llm-sched compile"
    - README.md has no Chinese characters (verified by unicode range check)
    - README.md has no references to SPEC-13, SPEC-14, SPEC-15, SPEC-16, SPEC-19, M1, M2, or v0.9
    - README.md has no references to diagnosis, visualization, sweep analysis, compare, evaluation reports, close-enough, or practical stop-line
  </acceptance_criteria>
  <done>README.md fully rewritten for v2 scope with warning banner, roadmap, and no legacy content.</done>
</task>

<task type="auto">
  <name>Task 2: Add numpy to pyproject.toml dependencies</name>
  <files>
    pyproject.toml
  </files>
  <read_first>
    - pyproject.toml (current content, updated in Plan 04)
  </read_first>
  <action>
    Add "numpy" to the [project] dependencies list in pyproject.toml. The dependencies section should become:

    ```toml
    dependencies = [
      "numpy>=1.26",
      "onnx>=1.20,<2",
      "pydantic>=2.12,<3",
      "typer>=0.24,<0.25",
    ]
    ```

    Keep Python >=3.11 as-is (D-24).
  </action>
  <verify>
    <automated>
      grep -q '"numpy>=1.26"' pyproject.toml && echo "OK numpy present" || (echo "FAIL" && exit 1)
      grep -q 'requires-python = ">=3.11"' pyproject.toml && echo "OK python version" || (echo "FAIL" && exit 1)
    </automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml dependencies list contains "numpy>=1.26"
    - pyproject.toml requires-python is still ">=3.11"
  </acceptance_criteria>
  <done>numpy added to dependencies; Python version constraint preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Commit README and dependency changes</name>
  <files>.git/</files>
  <read_first>
    - git status output from Tasks 1-2
  </read_first>
  <action>
    Stage all changes and commit:

    `git add -A && git commit -m "docs: rewrite README for v2 scope, add numpy dependency"`

    If pre-commit hooks fail, investigate and fix.
  </action>
  <verify>
    <automated>
      git log -1 --oneline | grep -q "docs: rewrite README for v2 scope, add numpy dependency"
    </automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --oneline` contains "docs: rewrite README for v2 scope, add numpy dependency"
    - `git status` is clean
  </acceptance_criteria>
  <done>README and dependency changes committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> user | README is the primary user-facing document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | README content | mitigate | No secrets or credentials in README. Warning banner prevents misuse of incomplete code. |
</threat_model>

<verification>
1. README.md passes all grep checks for required and forbidden content
2. pyproject.toml contains numpy>=1.26
3. pyproject.toml requires-python >=3.11
4. All changes committed
</verification>

<success_criteria>
- README.md states the project is undergoing complete refactoring
- README.md states current outputs are not correct
- README.md describes the 6-phase roadmap
- README.md has no v0.9-specific content
- README.md has no Chinese text
- README.md has no diagnosis, visualization, sweep analysis, compare, evaluation reports, close-enough, or practical stop-line references
- pyproject.toml includes numpy>=1.26 in dependencies
- pyproject.toml keeps requires-python >=3.11
- All changes committed
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup-foundation/01-05-SUMMARY.md`
</output>
